<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/custom_dialog_layout_design_user_input"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

   <TextView
       android:layout_width="wrap_content"
       android:layout_height="match_parent"
       android:layout_marginTop="15dp"
       android:fontFamily="@font/man_semi"
       android:gravity="center"
       android:text="Comment Reply"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:background="@drawable/layout_bg"
        android:paddingTop="10dp"
        android:paddingBottom="10dp">


        <EditText
            android:id="@+id/etReply"
            android:layout_width="0dp"
            android:layout_height="120dp"
            android:layout_centerHorizontal="true"
            android:layout_centerVertical="true"
            android:layout_margin="4dp"
            android:layout_weight="1"
            android:background="#FFFFFF"
            android:ems="10"
            android:fontFamily="@font/man_semi"
            android:gravity="top"
            android:hint="Please input your reply in the selected comment. Reminder! To prevent flooding you can reply as many times as you want but the latest reply is the only visible in the feedback section."
            android:inputType="text|textMultiLine|textCapSentences"
            android:paddingStart="10sp"
            android:paddingEnd="10sp"
            android:singleLine="false"
            android:textSize="13sp"></EditText>

    </LinearLayout>


</LinearLayout>